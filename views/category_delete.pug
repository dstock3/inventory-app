extends layout

block content
    div(class="main-subcontainer")
        h2 #{title}

        div(class="confirm-delete") Are you sure you want to delete this category?

        if items.length > 0
            p(class="warning") You must delete the following items before deleting this category.
            ul
                for item in items
                    li 
                        a(href=item.url) #{item.name}
        else 
            form(method='POST' action='')
                div.form-group
                    input#itemid.form-control(type='hidden',name='catid', required='true', value=category._id )
                    button.btn.btn-primary(type='submit') Confirm